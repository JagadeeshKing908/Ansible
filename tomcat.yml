---
- hosts: all
  connection: ssh
  vars:
   abc: httpd
  tasks:
    - name: install http
      yum: "{{abc}}" state=present
      notify: start httpd
  handlers:
    - name: start httpd
     service: name="{{abc}}" state=started
